<div class="row">
  <div class="col-xs-12 text-center">
    <h1 class="ml-3 mr-3">Meus Contatos</h1>
  </div>
</div>
<div class="row d-flex justify-content-center">
  <div class="col-md-12 col-md-offset-2 col-xs-12">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Nome</th>
          <th>Telefone</th>
          <th colspan="3">
            Ações
            <%= link_to 'Novo Contato', new_contact_path(), class: 'btn btn-success' %>
          </th>
        </tr>
      </thead>
      <tbody>
        <% if @contacts.empty? %>
          <tr>
            <td colspan="5" class="text-center">
              <h3>Sem contatos cadastrados.</h3>
              <%= link_to 'click aqui', new_contact_path() %> para cadastrar um novo contato
            </td>
          </tr>
        <% end %>
        <% @contacts.each do |c| %>
        <tr>
          <td>
            <%= c.name %>
          </td>
          <td>
            <%= c.phone %>
          </td>
          <td>
            <%= link_to 'Show', contact_path(c), class: 'btn btn-success' %>
          </td>
          <td>
            <%= link_to 'Editar', edit_contact_path(c), class: 'btn btn-warning' %>
          </td>
          <td>
            <%= link_to 'Excluir', contact_path(c), method: :delete, class: 'btn btn-danger', data: { confirm: 'Tem certeza que deseja excluir?' } %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>